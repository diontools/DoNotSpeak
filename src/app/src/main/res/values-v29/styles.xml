<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:colorPrimaryDark">@android:color/transparent</item>
    </style>

    <style name="ActivityTheme" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    </style>

    <!-- Disable animation for light mode. -->
    <style name="DisableAnimationDialogTheme" parent="android:Theme.DeviceDefault.Light.Dialog.Alert">
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>